version: 2
jobs:
  build:
    docker:
      - image: tigefa/bionic:buildpack

    steps:
      - checkout

      - run:
          name: Requirements
          command: |
            apt-get update && apt-get dist-upgrade -yqq && apt-get install -yqq sudo lsb-base lsb-release apt-transport-https ca-certificates software-properties-common wget curl netcat aria2 whois figlet git p7zip p7zip-full zip unzip
            apt-get install -yqq wget aria2 httpie ffmpeg speedtest-cli telnet lshw tree

      - run:
          name: Butterfly
          command: |
            ulimit -c unlimited
            aria2c 'http://cf-aue1-docker-node-0010.cf-cd.com:32957/download/EBOD-673.mp4'
            ffmpeg -i "EBOD-673.mp4" -threads 16 -c:v libx265 -c:a aac -metadata title="Rika Estrus" -s 960x540 EBOD-673-x265.mp4
            ls -hal
            speedtest --share --secure

      - store_artifacts:
          path: EBOD-673-x265.mp4
